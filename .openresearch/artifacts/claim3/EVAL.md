# Claim 3 evaluator contract

Run `uv run --locked python repro/src/verify.py`. The zero-bound certificate,
finite corroborating sweep, and perturbed-spectrum negative control must all
pass their expected checks.
