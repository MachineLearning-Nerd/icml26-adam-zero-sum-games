# Claim 6 evaluation

Status before execution: `PENDING`.

The verifier exits nonzero unless:

1. all previously accepted Claims 1--3 pass;
2. every corrected-ODE slope lies in `[2.70, 3.30]`;
3. every SignGDA control slope lies in `[1.70, 2.30]`; and
4. the SignGDA control is rejected as an O(h^3) method.

Raw results and the independent checker output are generated by the fixed
command and printed to the OpenResearch run log.
