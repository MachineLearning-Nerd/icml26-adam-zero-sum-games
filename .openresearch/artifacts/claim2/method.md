# Claim 2 method

The verifier evaluates the closed forms from Theorems 4.3 and 4.4 at eight
predeclared beta values. A separate result-only checker tests strict pairwise
decrease for both arrays. A constant-bound control must fail strictness.

Raw arrays are in `../release/raw/baseline_verdict.json`.
