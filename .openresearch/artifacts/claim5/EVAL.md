# Claim 5 evaluation

Current confidence: `LOW`.

Route 1 exits nonzero unless the exact headline settings are recovered, the
critical omissions are independently detected, and the route records BLOCKED
instead of treating paper-reported values as reproduced evidence.

Route 2 exits nonzero unless all four beta rank associations have the paper's
direction, all four rho tables are strictly monotone, nonmonotone beta pairs
are retained, the fixed-beta cross-table inconsistency is detected, and the
result remains BLOCKED rather than being called a reproduction.

The full claim remains unresolved until the required route sequence completes.

Route 3 exits nonzero unless all four architecture/shape pairs use batch 64,
the WGAN-GP training timings and gradient measurements are positive, the
forward-only control is faster, the HF CPU allocation is audited, and the
profile remains explicitly excluded from scientific claim evidence.
